//  for each loop

const prog = ["js", "cpp", "rb", "swift"]

prog.forEach(function (langs) {
  console.log(langs);

})// forEach loop attribute function doesnt have a name
console.log();

prog.forEach( (langs) => {console.log(langs)} )

log